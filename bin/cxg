#!/usr/bin/env bun
import { fileURLToPath } from "node:url"
import { runConduitGui } from "../packages/proxy/src/lib/codex-gui.ts"

process.exitCode = await runConduitGui(process.argv.slice(2), fileURLToPath(new URL("..", import.meta.url)))
